identity:
  name: recording_started_v1
  author: langgenius
  label:
    en_US: Recording Started
    zh_Hans: 录制开始
    ja_JP: 録画開始

description:
  en_US: This event activates when recording starts for a video conference meeting.
  zh_Hans: 当视频会议开始录制时激活。
  ja_JP: ビデオ会議の録画が開始されたときにアクティブになります。

parameters: []

output_schema:
  type: object
  properties:
    meeting_id:
      type: string
      description: ID of the meeting
    
    meeting_no:
      type: string
      description: Meeting number
    
    topic:
      type: string
      description: Meeting topic
    
    start_time:
      type: string
      description: Meeting start time
    
    meeting_source:
      type: string
      description: Meeting source
    
    calendar_event_id:
      type: string
      description: Calendar event ID
    
    host_user_id:
      type: string
      description: Host user ID
    
    host_user_type:
      type: string
      description: Host user type
    
    operator_id:
      type: string
      description: User who started recording
    
    operator_user_type:
      type: string
      description: Operator user type

extra:
  python:
    source: events/vc/recording_started_v1.py