identity:
  name: branch_protection_rule
  author: langgenius
  label:
    en_US: Branch Protection Rule
    zh_Hans: 分支保护规则
    ja_JP: ブランチ保護ルール

description:
  en_US: Triggers when branch protection rules change
  zh_Hans: 当分支保护规则变化时触发
  ja_JP: ブランチ保護ルールの変更でトリガー

parameters:
- name: actions
  label:
    en_US: Actions
    zh_Hans: 动作
    ja_JP: アクション
  type: select
  required: false
  multiple: true
  default:
  - created
  - edited
  - deleted
  options:
  - value: created
    label:
      en_US: Created
      zh_Hans: 已创建
      ja_JP: 作成
  - value: edited
    label:
      en_US: Edited
      zh_Hans: 已编辑
      ja_JP: 編集
  - value: deleted
    label:
      en_US: Deleted
      zh_Hans: 已删除
      ja_JP: 削除
  description:
    en_US: Filter which rule actions to process. Leave empty to accept created edited deleted.
    zh_Hans: 选择要处理的规则变更动作。留空接收 created edited deleted。
    ja_JP: ルール変更のアクションを選択。未設定で created edited deleted を許可。

- name: pattern
  label:
    en_US: Branch Pattern
    zh_Hans: 分支模式
    ja_JP: ブランチパターン
  type: string
  required: false
  description:
    en_US: Only trigger for these branch patterns (comma-separated)
    zh_Hans: 仅对这些分支模式触发（逗号分隔）
    ja_JP: これらのブランチパターンの場合のみトリガー（カンマ区切り）

output_schema:
  type: object
  properties:
    action:
      type: string
    rule:
      type: object
      properties:
        id:
          type: integer
        pattern:
          type: string
        repository_id:
          type: integer
      required:
      - id
      - pattern
    repository:
      type: object
      description: The repository where the rule changed
      properties:
        id:
          type: integer
          format: int64
        node_id:
          type: string
        name:
          type: string
        full_name:
          type: string
        private:
          type: boolean
        owner:
          type: object
          properties:
            id:
              type: integer
            login:
              type: string
            node_id:
              type: string
            avatar_url:
              type: string
              format: uri
            html_url:
              type: string
              format: uri
            type:
              type: string
              enum:
              - Bot
              - User
              - Organization
            site_admin:
              type: boolean
          required:
          - id
          - login
        html_url:
          type: string
          format: uri
        description:
          type:
          - string
          - "null"
        fork:
          type: boolean
        url:
          type: string
          format: uri
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        pushed_at:
          type:
          - string
          - "null"
          format: date-time
        stargazers_count:
          type: integer
        watchers_count:
          type: integer
        language:
          type:
          - string
          - "null"
        forks_count:
          type: integer
        open_issues_count:
          type: integer
        default_branch:
          type: string
        topics:
          type: array
          items:
            type: string
        visibility:
          type: string
          enum:
          - public
          - private
          - internal
      required:
      - id
      - node_id
      - name
      - full_name
      - private
      - owner
      - html_url
      - url
      - created_at
      - updated_at
      - fork
      - default_branch
      - visibility
    sender:
      type: object
      description: The user who triggered the event
      properties:
        id:
          type: integer
        login:
          type: string
        node_id:
          type: string
        avatar_url:
          type: string
          format: uri
        html_url:
          type: string
          format: uri
        type:
          type: string
          enum:
          - Bot
          - User
          - Organization
        site_admin:
          type: boolean
      required:
      - id
      - login
      - type

  required:
  - action
  - rule
  - repository
  - sender

extra:
  python:
    source: events/branch_protection_rule/branch_protection_rule.py
